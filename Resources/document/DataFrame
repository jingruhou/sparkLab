创建DataFrame
---通过json/parquet文件创建DataFrame
---通过RDD转化为DataFrame
---通过jdbc创建

通过反射推断包含了特定数据类型的RDD元数据，基于反射方式，代码比较简单，当已经知道RDD的元数据时，是非常不错的方式。
通过编程接口创建DataFrame,可以在程序运行时动态构建一份元数据，然后应用到存在的RDD上，适用于还不知道RDD的元数据，
只有在程序运行时，才能得知其元数据，就使用这种方法动态创建元数据的方式。